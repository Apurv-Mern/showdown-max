{
  "name": "showdown-trivia",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "logs:clear:local": "npm run logs:clear:local -w server",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "build:prod": "npm run build -w shared && npm run build -w server && npm run build:prod -w client",
    "lint": "eslint .",
    "load-test": "node tools/load-test/run.js",
    "load-test:setup": "node tools/load-test/run.js --setup --bots 150",
    "load-test:local": "node tools/load-test/run.js --setup --bots 30 --duration 120"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "dotenv": "^16.4.0",
    "socket.io-client": "^4.8.3"
  }
}
